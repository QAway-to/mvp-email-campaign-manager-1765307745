# 📬 Email Campaign Manager — Selling MVP  

Демонстрационная панель управления email‑рассылками. 75–80% готовности:  
клиент видит живой дашборд, мы подключаем реальный SMTP/ESP после оплаты.

## 🚀 Что уже реализовано

- **Дашборд кампании**: доставка, открытия, CTR, динамика на графиках  
- **Автоворонка**: визуальная схема письма → follow-up → напоминание  
- **Inbox мониторинг**: список входящих ответов, метки (лид/вопрос/отписка)  
- **Библиотека шаблонов**: ротация 5/10/15/20 отправок  
- **Настройки интеграций**: чек-лист по SendGrid/Mailgun/AWS SES  
- **Backend скрипт** `email_campaign_manager.py` с API SendGrid + IMAP мониторингом

## 🔧 Что остаётся подключить (доп. работа)

| Задача | Описание | Срок |
| ------ | -------- | ---- |
| Подключение ESP | API-ключи, доменная подпись, warm-up адресов | 1–2 дня |
| CRM/Telegram webhooks | Передача лидов в Bitrix24 / Slack / Telegram | 1 день |
| Расширенный отчёт | BigQuery / ClickHouse + PowerBI / Metabase | 2–3 дня |
| Автоответы | Настройка шаблонов ответов + сценарии | 1 день |

## 🗂️ Структура

```
email-campaign-manager/
├── pages/                # Next.js страницы (Dash, Campaigns, Templates, Settings)
├── src/components/       # UI-виджеты (графики, inbox, карточки)
├── src/mock-data/        # Моковые данные для демо
├── src/styles/global.css # Оформление
├── package.json          # Next.js 14 + Recharts
└── README.md
```

## ▶️ Запуск (Next.js)

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # сборка для Vercel
```

## 🧠 Скрипт кампаний

Файл `email_campaign_manager.py` уже реализует:

```python
async def main():
    manager = EmailCampaignManager()
    await manager.send_campaign_emails("contacts.csv")
    await manager.monitor_replies(
        email_account="sales@brand.dev",
        password="app_password"
    )
```

Подключаем SendGrid/Mailgun/AWS SES, Cron (Cloud Run / Railway) и CI/CD.

---

**Продвинутые апселлы**: персонализация контента, AMP for Email, deliverability‑аудит, интеграция с WhatsApp/SMS, стратегический контракт на 3–6 месяцев.  

Сгенерировано Agent B специально для «продающего» MVP 🚀

